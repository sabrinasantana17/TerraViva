// Estado inicial do baú
mensagem_exibida = false; // Controla se a mensagem já foi mostrada
mudas_liberadas = false;  // Controla se as mudas já foram liberadas

// Este baú não deve aparecer no início, ele será criado por código
// Se você arrastar ele para a room, certifique-se de torná-lo invisível
// (mas é melhor criá-lo via código para ter controle total).